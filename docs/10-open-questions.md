# Open Questions

These are the remaining items needed before implementation.

## Confirmed

1. Dujiao-Next source is cloned in the current project directory:
   - `dujiao-next/`
   - `user/`
   - `admin/`
   - `document/`
2. FansGurus API credentials have been provided out of band and must not be committed.
3. TGX credentials have been provided out of band and must not be committed.
4. TGX price markup uses `price`.
5. Launch payment channels should support Alipay, WeChat Pay, and PayPal through Dujiao-Next where practical.
6. Domain names are not decided yet.
7. Display and settlement currency is USD.
8. Project-level integration settings use one root `.env`.
9. Cloned Dujiao-Next source directories are not tracked by the root repository.

## Required Before Coding

1. Confirm whether Dujiao-Next runtime config should be manually copied from root `.env` during local setup or generated by a setup script.

## Recommended Before Launch

1. Should users be allowed to checkout without registration?
2. Should account delivery secrets be visible forever, or expire/hide after a period?
3. Should admin be able to override price multipliers per platform or per SKU?
4. Should unsupported FansGurus service types be hidden or displayed as unavailable?
5. Do we need a customer wallet/balance model, or only direct per-order payment?
6. Which countries/languages are SEO priorities beyond the three UI languages?
7. What refund and after-sales policy should be shown for TGX account products and FansGurus growth services?
8. What are the final storefront, admin, and callback domains?
9. What favicon, logo, OG image, and other brand assets should be used for each domain?

## Current Assumptions

- Telegram exclusion applies to both product catalog and customer-facing content.
- The platform intersection is calculated dynamically from current synced upstream catalogs.
- Public pages must never advertise platforms or SKUs that are filtered out by the intersection rule.
- Temporary development domain text can use placeholder domains and must be replaceable before launch.
